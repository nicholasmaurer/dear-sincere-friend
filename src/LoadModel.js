var THREE = require('three');


export default class LoadModel {

    constructor(webvr) {

        var scene = webvr.scene;
        var camera = webvr.camera;

        
    }
    animate() {
        
      }
}